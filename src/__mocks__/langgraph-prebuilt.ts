// Stub for @langchain/langgraph/prebuilt — used by moduleNameMapper in jest.config.js
// This file satisfies ts-jest's compilation step.
// The actual mock behaviour is overridden by jest.mock(..., factory) in each test file.
export const createReactAgent = jest.fn()
